apiVersion: apps/v1
kind: Deployment
metadata:
  name: green
  labels:
    app: green
spec:
  selector:
    matchLabels:
      app: green
  template:
    metadata:
      labels:
        app: green
    spec:
      containers:
      - name: nginx
        image: hiakki/my-nginx
        volumeMounts:
        - mountPath: /var/log/nginx/
          name: logs
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 10m
      - name: php-fpm
        image: hiakki/my-php
        ports:
        - containerPort: 9000
        resources:
          requests:
            cpu: 10m
      volumes:
      - name: logs
        hostPath:
          path: /home/kube/logs
