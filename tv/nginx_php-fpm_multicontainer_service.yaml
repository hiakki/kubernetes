apiVersion: v1
kind: Service              
metadata:
  name: tv

spec:
  type: LoadBalancer
  externalIPs:
  - 192.168.1.18
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30420
  selector:
    app: tv
